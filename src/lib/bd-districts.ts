// Compatibility entry point for shipping and existing imports. The canonical
// district list now comes from the same hierarchy as the linked checkout fields.
export { BD_DISTRICTS, BD_DISTRICT_SET } from './bd-locations'
